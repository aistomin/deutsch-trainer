version: 1.0.{build}
skip_tags: true
branches:
  only:
    - master
image: Ubuntu1804
services:
  - postgresql
environment:
  PGUSER: postgres
  PGPASSWORD: Password12!
build_script:
  - echo $PWD $USER $PGUSER $PGPASSWORD
  - sudo psql -c 'create database my_test_db;'
